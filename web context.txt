================================================================================
PROJECT: MedQ&A RAG Chatbot
PATH: /Users/dhairya/Projects/RAG_ChatBot/phase2_medqa_project/
================================================================================

STACK:
  Python 3.14 | FastAPI | Qdrant | Sentence Transformers | LangChain
  Mistral API (mistral-small-latest) | Streamlit | Docker

PHASES: 1✅ 2✅ 3✅ 4✅ 5=IN PROGRESS

================================================================================
SYSTEM ARCHITECTURE
================================================================================

INGESTION (done, 229,717 points in Qdrant):
  HuggingFace (lavita/medical-qa-datasets, all-processed)
  → cleaner → chunker (400 tok, 50 overlap) → all-MiniLM-L6-v2 (384-dim)
  → Qdrant (medquad_chunks, cosine)

INFERENCE (3 paths):
  User → Streamlit → api_client.py → FastAPI /api/v1/chat → agent.py
  Path A (default): embed → Qdrant top-5 → Mistral → answer+sources
  Path B (PDF):     PyMuPDF → embed → Qdrant → Mistral → explanation
  Path C (drug):    regex detect → drugs.com scrape / FDA API → Mistral
                    ⚠ CHANGING: move to RAG (supervisor decision)

SERVICES (Docker):
  qdrant    :6333  (medquad_chunks, 229,717 points, volume: qdrant_storage)
  api       :8000  (FastAPI, 4 workers)
  frontend  :8501  (Streamlit)
  Mistral   cloud  (no TPM limit, ~2-3s/request, 50 concurrent confirmed)

================================================================================
KEY FILES
================================================================================

app/utils/config.py       mistral_api_key, llm_model=mistral-small-latest
app/utils/logger.py       structlog (add_logger_name removed)
app/services/embedding.py async, thread pool, all-MiniLM-L6-v2
app/services/retrieval.py async, query_points() (qdrant-client 1.17+)
app/services/llm.py       async, ChatMistralAI
app/services/agent.py     async, strict drug regex + 50-word exclusion list
app/api/routes/chat.py    await agent.run() on both endpoints
app/ingestion/loader.py   config="all-processed", no trust_remote_code
app/frontend/app.py       Streamlit dark theme
app/frontend/api_client.py BASE_URL from env (API_BASE_URL)

================================================================================
NEXT TASK: DRUG DATA INGESTION (supervisor decision)
================================================================================

PROBLEM: drug_tool.py does live scraping → adds 2-5s latency
SOLUTION: scrape drugs once → store in Qdrant → serve via RAG

DECISIONS NEEDED FROM USER:
  1. Which drugs? (top 100 / top 500 / custom list)
  2. Source? FDA API (reliable) or drugs.com (blocks 403s)
  3. Same collection (medquad_chunks) or new (drug_chunks)?
  4. Keep drug_tool.py as fallback or remove?

PLAN (once decisions made):
  - Write drug_ingestion.py (scrape → chunk → embed → upsert)
  - Update agent.py (remove drug regex routing → all goes to RAG)
  - Keep/remove drug_tool.py based on decision 4

================================================================================
PENDING: PHASE 5 EVALUATION
================================================================================

METRICS TO COMPUTE:
  Retrieval: Precision@K, Recall@K, MRR, NDCG
  Answer:    ROUGE-1, ROUGE-2, ROUGE-L, BLEU
  Semantic:  cosine similarity (answer vs ground truth)
  Routing:   Precision, Recall, F1, AUC (drug/RAG/report classification)

NEEDS FROM USER BEFORE WRITING:
  - Ground truth = MedQuAD original answers ✓
  - Test set size (100 / 500 / all)
  - pip show rouge-score scikit-learn (check if installed)
  - app/models/schemas.py (paste before writing eval script)

================================================================================
KNOWN ISSUES
================================================================================

  - Docker: only Qdrant starts automatically (healthcheck issue — TBD)
  - UI: "not printing properly" in Docker (details needed)
  - drug_tool: drugs.com returns 403 → falls back to FDA API

================================================================================
HOW TO RESUME
================================================================================

  Share this file + say what to work on next.
  Before writing any file, Claude will ask for current version first.

================================================================================